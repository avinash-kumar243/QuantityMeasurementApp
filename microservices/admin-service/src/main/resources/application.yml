server:
  port: ${PORT:8080}

spring:
  application:
    name: ADMIN-SERVICE
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
management:
  endpoints:
    web:
      exposure:
        include: health,info